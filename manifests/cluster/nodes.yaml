# danavira-vps-1: control plane — no taints, runs cluster-wide tooling
# (ArgoCD, Prometheus, Grafana, Radar, ingress, cert-manager)

# danavira-vps-2: stateful node — Postgres, ClickHouse
apiVersion: v1
kind: Node
metadata:
  name: danavira-vps-2
  labels:
    role: stateful
spec:
  taints:
    - key: role
      value: stateful
      effect: NoSchedule
---
# danavira-vps-3: compute node — Spark, Airflow, Kafka, Debezium
apiVersion: v1
kind: Node
metadata:
  name: danavira-vps-3
  labels:
    role: compute
spec:
  taints:
    - key: role
      value: compute
      effect: NoSchedule
