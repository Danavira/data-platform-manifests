# Network policies per namespace.
# Default pattern: deny all ingress/egress, then explicitly allow what's needed.

# --- postgres ---
# TODO: allow ingress from airflow namespace on port 5432

# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: default-deny-ingress
#   namespace: postgres
# spec:
#   podSelector: {}
#   policyTypes:
#     - Ingress
#     - Egress

# --- airflow ---
# TODO: allow ingress from ingress-nginx on port 8080 (webserver)

# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: default-deny-ingress
#   namespace: airflow
# spec:
#   podSelector: {}
#   policyTypes:
#     - Ingress
#     - Egress
